name: lables
on:
#  push:
  pull_request:
    branches:
      - uk_sprint*
jobs:
  run_if_label_matches:
   # if: github.event.label.name == 'bug'
    runs-on: ubuntu-latest
    steps:
      - run: echo 'The label was bug'
